import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miempresa.esimedia',
  appName: 'esimedia-frontend',
  webDir: 'dist/esimedia-frontend/browser',
};

export default config;
