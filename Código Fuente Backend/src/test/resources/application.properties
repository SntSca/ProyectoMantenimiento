# Test configuration - dummy values for required environment variables
spring.application.name=ESIMedia

# Test database configuration (in-memory or embedded)
spring.data.mongodb.uri=mongodb://localhost:27017/testdb
spring.data.mongodb.database=testdb

# Test JWT configuration
token.secret=test-jwt-secret-key-for-testing-purposes-only
token.expiration=86400000

# Disable antivirus for tests
antivirus.enabled=false

# Test email configuration (dummy values)
spring.mail.host=localhost
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=testpassword
email.remitente=test@example.com
email.passwordApp=testpassword
email.confirmationUrl=http://localhost:9090/users/register/confirm

# Security pepper for tests
app.security.pepper=test-pepper-for-testing

# Disable MongoDB auto-index creation for tests
spring.data.mongodb.auto-index-creation=false

# Logging configuration for tests
logging.level.org.springframework=INFO
logging.level.com.esimedia=DEBUG